#include <bits/stdc++.h>

#ifdef LOCAL
#define debug(args...) std::println(stderr, "({}) = {}", #args, std::tuple(args))
#else
#define debug(...) ((void)0)
#endif

int main() {
  std::ios::sync_with_stdio(false);
  std::cin.tie(nullptr);



}
